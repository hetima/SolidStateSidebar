<UserControl x:Class="SSS.Module.WindowMonitor.Section"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="clr-namespace:SSS.Core"
             xmlns:local="clr-namespace:SSS.Module.WindowMonitor"
             xmlns:components="clr-namespace:SSS.Views.Components"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="200">
    <StackPanel Style="{StaticResource GroupPanel}">
        <components:ModuleHeader />

        <ItemsControl ItemsSource="{Binding Path=Monitors, Mode=OneWay}">
            <ItemsControl.Resources>
                <DataTemplate DataType="{x:Type core:BaseMonitor}">
                    <ItemsControl ItemsSource="{Binding Path=Windows, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:WindowItem}">
                                <Button Command="{Binding Path=ActivateCommand}"
                                        Visibility="{Binding Path=Visibility, Mode=OneWay}"
                                        Padding="8,4"
                                        HorizontalContentAlignment="Left"
                                        Content="{Binding Path=Title, Mode=OneWay}"
                                        Cursor="Hand"
                                        FontSize="{Binding Source={x:Static core:Settings.Instance}, Path=FontSize, Mode=OneWay}"
                                        Foreground="{Binding Source={x:Static core:Settings.Instance}, Path=FontColor, Mode=OneWay}"
                                        Background="Transparent"
                                        BorderThickness="1,1,1,1"
                                        ToolTip="{Binding Path=ProcessName, Mode=OneWay}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Left" />
                                            <Setter Property="Foreground" Value="{Binding Source={x:Static core:Settings.Instance}, Path=FontColor, Mode=OneWay}" />
                                            <Setter Property="Background" Value="Transparent" />
                                            <Setter Property="BorderThickness" Value="1,1,1,1" />
                                            <Setter Property="BorderBrush" Value="Transparent" />
                                            <Setter Property="Cursor" Value="Hand" />
                                            <Setter Property="Content" Value="{Binding Path=Title, Mode=OneWay}" />
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type Button}">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                SnapsToDevicePixels="True">
                                                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="BorderBrush" Value="{Binding Source={x:Static core:Settings.Instance}, Path=FontColor, Mode=OneWay}" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Path=IsMinimized, Mode=OneWay}" Value="True">
                                                    <Setter Property="Opacity" Value="0.5" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Style="{StaticResource VerticalPanel}" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </DataTemplate>
            </ItemsControl.Resources>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Style="{StaticResource VerticalPanel}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</UserControl>
