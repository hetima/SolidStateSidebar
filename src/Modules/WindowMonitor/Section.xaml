<UserControl x:Class="SSS.Module.WindowMonitor.Section"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="clr-namespace:SSS.Core"
             xmlns:local="clr-namespace:SSS.Module.WindowMonitor"
             xmlns:components="clr-namespace:SSS.Views.Components"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="200">
    <StackPanel Style="{StaticResource GroupPanel}">
        <components:ModuleHeader />

        <ItemsControl ItemsSource="{Binding Path=Monitors, Mode=OneWay}">
            <ItemsControl.Resources>
                <DataTemplate DataType="{x:Type core:BaseMonitor}">
                    <ItemsControl ItemsSource="{Binding Path=Windows, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:WindowItem}">
                                <Button Command="{Binding Path=ActivateCommand}"
                                        Visibility="{Binding Path=Visibility, Mode=OneWay}"
                                        Padding="8,4"
                                        Cursor="Hand"
                                        ToolTip="{Binding Path=ProcessName, Mode=OneWay}">

                                    <!-- 1. スタイル定義（重複を削り、ルールだけをシンプルに記述） -->
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="FocusVisualStyle"
                                                    Value="{x:Null}"/>
                                            <Setter Property="HorizontalContentAlignment"
                                                    Value="Left"/>
                                            <Setter Property="Foreground"
                                                    Value="{Binding Source={x:Static core:Settings.Instance}, Path=FontColor, Mode=OneWay}"/>
                                            <Setter Property="Background"
                                                    Value="Transparent"/>
                                            <Setter Property="BorderThickness"
                                                    Value="1"/>
                                            <Setter Property="BorderBrush"
                                                    Value="Transparent"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type Button}">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                SnapsToDevicePixels="True">
                                                            <!-- 内容（アイコンとテキスト）をここに表示 -->
                                                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver"
                                                                    Value="True">
                                                                <Setter Property="BorderBrush"
                                                                        Value="{Binding Source={x:Static core:Settings.Instance}, Path=FontColor, Mode=OneWay}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <!-- 最小化のときはボタンを半透明にする -->
                                                <DataTrigger Binding="{Binding Path=IsMinimized, Mode=OneWay}"
                                                        Value="True">
                                                    <Setter Property="Opacity"
                                                            Value="0.5"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <!-- 2. ボタンの中身（Content）: 重複を解消し、Gridで文字溢れと色連動を完璧に対応 -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <!-- アイコン用（サイズぴったりに縮める） -->
                                            <ColumnDefinition Width="*"/>
                                            <!-- テキスト用（残りの横幅をすべて使う） -->
                                        </Grid.ColumnDefinitions>

                                        <!-- 左側：アイコン（OpacityMaskを使ってボタンの文字色と完全連動） -->
                                        <Image Grid.Column="0"
                                               Source="{Binding ProcessIcon, Mode=OneWay}"
                                               Width="16"
                                               Height="16"
                                               Margin="0,0,6,0"
                                               Stretch="Uniform"/>

                                        <!-- 右側：ウィンドウタイトル（文字が長いときは自動で ... になる） -->
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Path=Title, Mode=OneWay}"
                                                   TextTrimming="CharacterEllipsis"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Style="{StaticResource VerticalPanel}" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </DataTemplate>
            </ItemsControl.Resources>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Style="{StaticResource VerticalPanel}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</UserControl>
