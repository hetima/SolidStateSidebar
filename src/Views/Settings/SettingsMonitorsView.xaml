<UserControl x:Class="SidebarDiagnostics.SettingsMonitorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
             xmlns:monitor="clr-namespace:SidebarDiagnostics.Core"
             xmlns:conv="clr-namespace:SidebarDiagnostics.Converters"
             xmlns:res="clr-namespace:SidebarDiagnostics"
             mc:Ignorable="d">
    <UserControl.Resources>
        <conv:IntToStringConverter x:Key="IntConverter" />
    </UserControl.Resources>
    <StackPanel Style="{StaticResource VerticalPanel}">
        <StackPanel Style="{StaticResource SettingTitle}">
            <TextBlock Text="{x:Static res:Strings.SettingsMonitorsSubtitle1}" />
            <TextBlock Text="{x:Static res:Strings.SettingsMonitorsSubtitle2}" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding Path=MonitorConfig, Mode=OneWay}" dd:DragDrop.IsDragSource="True" dd:DragDrop.IsDropTarget="True" Style="{StaticResource MonitorGrid}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="{x:Static res:Strings.SettingsMonitorsColumn1}" HeaderStyle="{StaticResource MonitorGridHeaderCenter}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="{x:Type monitor:MonitorConfig}">
                            <CheckBox IsChecked="{Binding Path=Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="{x:Static res:Strings.SettingsMonitorEnabledTooltip}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="{x:Static res:Strings.SettingsMonitorsColumn2}" Width="*" HeaderStyle="{StaticResource MonitorGridHeader}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="{x:Type monitor:MonitorConfig}">
                            <Label Content="{Binding Path=Name}" VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource MonitorDetailsBorder}">
                        <StackPanel Style="{StaticResource VerticalPanel}">
                            <ListView ItemsSource="{Binding Path=HardwareOC, Mode=OneWay}" dd:DragDrop.IsDragSource="True" dd:DragDrop.IsDropTarget="True" ItemContainerStyle="{StaticResource MonitorHardwareItem}" Style="{StaticResource HardwareGrid}">
                                <ListView.View>
                                    <GridView AllowsColumnReorder="False">
                                        <GridViewColumn Header="{x:Static res:Strings.SettingsHardwareColumn1}" HeaderContainerStyle="{StaticResource MonitorHardwareHeaderCenter}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type monitor:HardwareConfig}">
                                                    <CheckBox IsChecked="{Binding Path=Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="{x:Static res:Strings.SettingsHardwareEnabledTooltip}" Style="{StaticResource MonitorHardwareCheckbox}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="{x:Static res:Strings.SettingsHardwareColumn2}" HeaderContainerStyle="{StaticResource MonitorHardwareHeader}" Width="230">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type monitor:HardwareConfig}">
                                                    <TextBox Text="{Binding Path=Name, Mode=TwoWay}" Style="{StaticResource MonitorHardwareText}" ToolTip="{x:Static res:Strings.SettingsHardwareNameTooltip}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>

                            <DockPanel LastChildFill="True" Margin="0,15,0,0">
                                <Label DockPanel.Dock="Left" Content="{x:Static res:Strings.SettingsMetrics}" Margin="0,0,10,0" />
                                <xctk:CheckComboBox ItemsSource="{Binding Path=Metrics, Mode=OneWay}" DisplayMemberPath="Name" SelectedMemberPath="Enabled" Delimiter=", " ToolTip="{x:Static res:Strings.SettingsMetricsTooltip}" />
                            </DockPanel>

                            <ItemsControl ItemsSource="{Binding Path=Params}" Margin="10">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type monitor:ConfigParam}">
                                        <ContentControl>
                                            <ContentControl.Style>
                                                <Style TargetType="{x:Type ContentControl}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding TypeString}" Value="System.Boolean">
                                                            <Setter Property="Content">
                                                                <Setter.Value>
                                                                    <CheckBox Content="{Binding Path=Name}" IsChecked="{Binding Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding Path=Tooltip, Mode=OneTime}" />
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding TypeString}" Value="System.Int32">
                                                            <Setter Property="Content">
                                                                <Setter.Value>
                                                                    <StackPanel>
                                                                        <Label Content="{Binding Path=Name, Mode=OneTime}" />
                                                                        <TextBox Text="{Binding Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource IntConverter}}" ToolTip="{Binding Path=Tooltip, Mode=OneTime}" />
                                                                    </StackPanel>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ContentControl.Style>
                                            <ContentControl.Resources>
                                                <Style TargetType="{x:Type CheckBox}">
                                                    <Setter Property="VerticalAlignment" Value="Center" />
                                                    <Setter Property="Margin" Value="0,5" />
                                                </Style>
                                                
                                                <Style TargetType="{x:Type StackPanel}">
                                                    <Setter Property="Orientation" Value="Horizontal" />
                                                    <Setter Property="Margin" Value="0,5" />
                                                    <Style.Resources>
                                                        <Style TargetType="{x:Type Label}">
                                                            <Setter Property="VerticalAlignment" Value="Center" />
                                                            <Setter Property="Margin" Value="0,0,10,0" />
                                                            <Setter Property="Padding" Value="0" />
                                                        </Style>
                                                        <Style TargetType="{x:Type TextBox}">
                                                            <Setter Property="VerticalAlignment" Value="Center" />
                                                            <Setter Property="Width" Value="40" />
                                                            <Setter Property="MaxLines" Value="1" />
                                                        </Style>
                                                    </Style.Resources>
                                                </Style>
                                            </ContentControl.Resources>
                                        </ContentControl>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Style="{StaticResource VerticalPanel}" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>
    </StackPanel>
</UserControl>