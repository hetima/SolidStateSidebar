<sty:FlatWindow
        x:Class="SidebarDiagnostics.Update"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SidebarDiagnostics"
        xmlns:win="clr-namespace:SidebarDiagnostics.Windows"
        xmlns:core="clr-namespace:SidebarDiagnostics.Core"
        xmlns:sty="clr-namespace:SidebarDiagnostics.Style"
        xmlns:res="clr-namespace:SidebarDiagnostics"
        mc:Ignorable="d"
        Title="{x:Static res:Strings.AppName}"
        Icon="../Resources/Sidebar.ico"
        Width="300"
        SizeToContent="Height"
        ShowTitleBar="False"
        ShowIcon="False"
        ShowTitle="False"
        ShowMinimize="False"
        ShowMaximize="False"
        ShowClose="False"
        Topmost="False"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource FlatWindowStyle}">
    <Window.TaskbarItemInfo>
        <TaskbarItemInfo ProgressState="Normal" ProgressValue="{Binding Path=ProgressNormalized, Mode=OneWay}" />
    </Window.TaskbarItemInfo>
    <DockPanel LastChildFill="True">
        <Label DockPanel.Dock="Top" Content="{x:Static res:Strings.UpdateTitle}" Style="{StaticResource UpdateTitle}" />
        <ProgressBar Value="{Binding Path=Progress, Mode=OneWay}" Style="{StaticResource UpdateProgress}" />
    </DockPanel>
</sty:FlatWindow>
