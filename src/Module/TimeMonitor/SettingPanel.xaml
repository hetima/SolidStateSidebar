<UserControl x:Class="SSS.Module.TimeMonitor.SettingPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:monitor="clr-namespace:SSS.Core"
             xmlns:conv="clr-namespace:SSS.Converters"
             xmlns:res="clr-namespace:SSS"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d">
    <UserControl.Resources>
        <conv:DateFormatConverter x:Key="DateFormatConverter" />
        <conv:DateFormatDisplayConverter x:Key="DateFormatDisplayConverter" />
        <conv:IntToStringConverter x:Key="IntConverter" />

        <!-- DateFormat ComboBox items -->
        <x:Array x:Key="DateFormatItems" Type="sys:Int32">
            <sys:Int32>0</sys:Int32>
            <sys:Int32>1</sys:Int32>
            <sys:Int32>2</sys:Int32>
            <sys:Int32>3</sys:Int32>
        </x:Array>

        <DataTemplate x:Key="DateFormatItemTemplate">
            <TextBlock Text="{Binding Converter={StaticResource DateFormatDisplayConverter}}" />
        </DataTemplate>
    </UserControl.Resources>
    <Border Style="{StaticResource MonitorDetailsBorder}">
        <StackPanel Style="{StaticResource VerticalPanel}">
            <!-- Metrics -->
            <DockPanel LastChildFill="True" Margin="0,0,0,0">
                <Label DockPanel.Dock="Left" Content="{x:Static res:Strings.SettingsMetrics}" Margin="0,0,10,0" />
                <xctk:CheckComboBox ItemsSource="{Binding Path=Metrics, Mode=OneWay}" DisplayMemberPath="Name" SelectedMemberPath="Enabled" Delimiter=", " ToolTip="{x:Static res:Strings.SettingsMetricsTooltip}" />
            </DockPanel>

            <!-- Params: Clock24HR (Boolean), DateFormat (ComboBox) -->
            <ItemsControl ItemsSource="{Binding Path=Params}" Margin="10">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type monitor:ConfigParam}">
                        <ContentControl>
                            <ContentControl.Style>
                                <Style TargetType="{x:Type ContentControl}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding TypeString}" Value="System.Boolean">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <CheckBox Content="{Binding Path=Name}" IsChecked="{Binding Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding Path=Tooltip, Mode=OneTime}" />
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding TypeString}" Value="System.Int32">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <StackPanel>
                                                        <Label Content="{Binding Path=Name, Mode=OneTime}" />
                                                        <TextBox Text="{Binding Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource IntConverter}}" ToolTip="{Binding Path=Tooltip, Mode=OneTime}" />
                                                    </StackPanel>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Key}" Value="{x:Static monitor:ParamKey.DateFormat}">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <StackPanel>
                                                        <Label Content="{Binding Path=Name, Mode=OneTime}" />
                                                        <ComboBox ItemsSource="{StaticResource DateFormatItems}"
                                                                  SelectedValue="{Binding Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource DateFormatConverter}}"
                                                                  SelectedValuePath="."
                                                                  ItemTemplate="{StaticResource DateFormatItemTemplate}"
                                                                  ToolTip="{Binding Path=Tooltip, Mode=OneTime}" />
                                                    </StackPanel>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ContentControl.Style>
                            <ContentControl.Resources>
                                <Style TargetType="{x:Type CheckBox}">
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="Margin" Value="0,5" />
                                </Style>
                                <Style TargetType="{x:Type StackPanel}">
                                    <Setter Property="Orientation" Value="Horizontal" />
                                    <Setter Property="Margin" Value="0,5" />
                                    <Style.Resources>
                                        <Style TargetType="{x:Type Label}">
                                            <Setter Property="VerticalAlignment" Value="Center" />
                                            <Setter Property="Margin" Value="0,0,10,0" />
                                            <Setter Property="Padding" Value="0" />
                                        </Style>
                                        <Style TargetType="{x:Type TextBox}">
                                            <Setter Property="VerticalAlignment" Value="Center" />
                                            <Setter Property="Width" Value="40" />
                                            <Setter Property="MaxLines" Value="1" />
                                        </Style>
                                    </Style.Resources>
                                </Style>
                            </ContentControl.Resources>
                        </ContentControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Style="{StaticResource VerticalPanel}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
