<UserControl x:Class="SSS.Module.TimeMonitor.Section"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="clr-namespace:SSS.Core"
             xmlns:styling="clr-namespace:SSS.Styling"
             mc:Ignorable="d">
    <StackPanel Style="{StaticResource GroupPanel}">
        <styling:ModuleHeader />

        <ItemsControl ItemsSource="{Binding Path=Monitors, Mode=OneWay}">
            <ItemsControl.Resources>
                <DataTemplate DataType="{x:Type core:BaseMonitor}">
                    <StackPanel Style="{StaticResource MonitorPanel}">
                        <ItemsControl ItemsSource="{Binding Path=Metrics, Mode=OneWay}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type core:iMetric}">
                                    <TextBlock Text="{Binding Path=Text, Mode=OneWay}" Style="{StaticResource MetricValue}" FontSize="{Binding Path=FontSize, Mode=OneWay}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Style="{StaticResource VerticalPanel}" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.Resources>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Style="{StaticResource VerticalPanel}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</UserControl>
